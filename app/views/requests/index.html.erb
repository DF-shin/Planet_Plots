<h1>This is the <%= @user.first_name %>'s requests list</h1>
<ul>
  <% @requests.each do |request| %>
  <li>
    Plot name: <%= request.plot.name %>  Buyer name: <%= request.plot.user.first_name %>
  </li>
  <% end %>
</ul>
